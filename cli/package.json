{
  "name": "llm-bridge",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "llm-bridge": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@llm-bridge/copilot": "workspace:*",
    "@llm-bridge/core": "workspace:*",
    "@llm-bridge/cursor": "workspace:*",
    "@llm-bridge/windsurf": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "vitest": "^2.0.0"
  }
}
